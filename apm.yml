name: save_redirect
version: 0.0.0
description: Reproducible agent skills for SaveRedirect maintenance
author: aoirint
# Which agent platforms to deploy to.
# Resolution order: --target flag > this field > auto-detect from filesystem.
# Accepted values: vscode, agents, copilot, claude, cursor, opencode, codex,
# gemini, antigravity, windsurf, kiro, agent-skills, all
targets:
  - codex
dependencies:
  apm:
    - git: aoirint/skills
      ref: e5adf74a632936834a31a7cc2507030b362a7210
      skills:
        - apm-workflow
        - bepinex-mono-mod-quality-check
        - changelog-workflow
        - code-quality-check
        - commit-message-quality-check
        - git-worktree-workflow
        - github-actions-quality-check
        - gitignore-workflow
        - github-workflow
        - lethal-company-analyze
        - prose-quality-check
        - release-note-workflow
        - rollout-workflow
        - security-check
        - domain-architecture-docs-workflow
        - test-quality-check
        - unity-game-analyze
  mcp: []
includes: auto
scripts: {}
